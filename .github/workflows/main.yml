name: secrets and variables
on: workflow_dispatch
jobs:
  secrets:
    runs-on: ubuntu-latest
    env:
      USERNAME: Admin
      PASSWORD: Admin123
    steps: 
       - name: login database
         run: echo "login database using password $PASSWORD and username $USERNAME"
    
